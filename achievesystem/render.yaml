static_sites:
  - name: achievesystem
    type: web
    buildCommand: npm run build
    staticPublishPath: ./dist  # Vite outputs to 'dist', not 'build'
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: CI
        value: false